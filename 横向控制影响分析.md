# 横向控制对纵向控制的影响分析

## 版本

**Git**: `60697ec2e1`

## 关键发现

根据你的观察："机翼有向上的力，但飞机还在下降"

这可能是**大横滚角导致垂直升力不足**！

### 升力分解原理

固定翼飞机在倾斜时，升力被分解为：
```
垂直升力 = 总升力 × cos(横滚角)
侧向力   = 总升力 × sin(横滚角)
```

### 横滚角的影响

| 横向加速度 | 横滚角 | 垂直升力保持 | 升力损失 |
|-----------|--------|------------|---------|
| 3 m/s²    | ~17°   | cos(17°) = 96% | 4% |
| 6 m/s²    | ~31°   | cos(31°) = 86% | **14%** |
| 9 m/s²    | ~43°   | cos(43°) = 73% | **27%** |

### 问题分析

如果飞机在低速/低空时：
- 升力本来就勉强够（刚好平衡重力）
- L1指令大横向加速度（6 m/s²）
- 横滚角达到31度
- 垂直升力损失14%
- **垂直升力 < 重力 → 飞机下降！**

即使TECS指令爬升：
- TECS拉高机头
- 但大横滚角导致升力垂直分量不足
- 结果：有升力但还下降

## 已实施的修复

### 1. 减少L1横向加速度限制

```cpp
// 之前：
float max_lateral_accel = 6.0f;  // 横滚角31度，损失14%升力

// 现在：
float max_lateral_accel = 3.0f;  // 横滚角17度，损失4%升力
```

### 2. 添加模式切换调试

```cpp
AUTO_POSITION: alt_sp=XXX, airspeed_sp=XXX, height_rate=NAN, curr_alt=XXX
```

这会显示：
- 进入AUTO模式时的高度设定值
- 是否使用了height_rate（应该是NAN）
- 当前实际高度

## 测试要点

```bash
cd /home/jjs/PX4/PX4-Autopilot  
make clean
make px4_sitl gazebo-classic
```

### 观察1：横向加速度

在Gazebo中观察：
- 飞机的横滚角是否小于20度？
- 如果还是大横滚（>25度），说明L1还有问题

### 观察2：模式切换

看日志：
```
INFO  [commander] Takeoff detected
WARN  [fw_mode_manager] AUTO_POSITION: alt_sp=530.8, airspeed_sp=15.0, height_rate=NAN, curr_alt=506.7
```

检查：
- `alt_sp`是否正确（应该是530.8米）
- `curr_alt`是多少（应该在500左右）
- 切换后飞机是爬升还是下降？

## 可能的结果

### 结果A：减少横滚角后正常

- 飞机能保持/爬升高度
- 说明确实是横滚角过大导致升力不足
- ✅ 问题解决

### 结果B：仍然下降

- 说明不是横滚角问题
- 可能是TECS参数或模式切换问题
- 需要进一步诊断

## 下一步

如果还下降，请提供：
1. AUTO_POSITION的调试输出（alt_sp, curr_alt等）
2. Gazebo中观察到的横滚角大小
3. 是否看到"Waypoint cruising_speed not set"警告？

这些信息将帮助我们精确定位问题！

