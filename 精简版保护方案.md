# 精简版保护方案 - 解决俯仰振荡和着陆失控

## 问题总结

基于你的反馈：
1. ✅ 干净版本（无保护）：飞到第一航点中间还是会触地
2. ❌ 完整保护版本：俯仰角波动剧烈 + 着陆时失控

## 新方案：精简保护

### 核心思路

只保留**最关键的低高度保护**，去除所有可能引起振荡的机制：
- ❌ 移除硬地线强制爬升（可能导致振荡）
- ❌ 移除FOH相同高度检测（可能干扰TECS）
- ❌ 移除心跳调试输出（减少噪音）
- ❌ 移除大量ERROR/WARN日志（减少干扰）
- ✅ 只保留简单的低高度保护

### 已实施的修改

```cpp
// 简化的低高度保护：只在AGL<25米时温和调整
const float agl = -_local_pos.z;
const float MIN_SAFE_AGL = 25.0f;

if (!_landed && (agl < MIN_SAFE_AGL) && (position_sp_alt < _current_altitude)) {
    const float safe_altitude = _current_altitude + (MIN_SAFE_AGL - agl);
    position_sp_alt = safe_altitude;
}
```

**特点**：
- 25米触发（不会太高导致频繁触发）
- 温和调整（不强制20度爬升）
- 无日志输出（避免噪音）
- 让TECS自然响应

## 下一步调试策略

### 问题1：俯仰角波动剧烈

**可能原因**：
1. TECS参数不合适
2. 空速控制不稳定
3. 高度参考点跳变

**建议调试**：
```bash
# 检查TECS参数
param show FW_T_SPDWEIGHT  # 速度权重
param show FW_T_CLMB_R_SP  # 爬升率
param show FW_T_SINK_R_SP  # 下降率

# 如果俯仰波动大，可以尝试：
param set FW_T_SPDWEIGHT 1.0  # 平衡高度和速度控制
```

**观察点**：
- 俯仰波动发生在什么阶段？（爬升？平飞？下降？）
- 波动幅度是多少？（±5度？±10度？）
- 速度是否也在波动？

### 问题2：着陆时失控

**可能原因**：
1. 着陆进近速度过快
2. 下滑角过陡
3. L1/NPFG在低速时不稳定

**建议**：
1. **检查着陆航点设置**：
   ```
   着陆航点类型：LAND
   进近高度：足够高（如100米）
   下滑角：不要太陡（<5度）
   ```

2. **着陆参数**：
   ```bash
   param show FW_LND_ANG      # 着陆下滑角
   param show FW_LND_AIRSPD   # 着陆空速
   param show FW_LND_FLALT    # 拉平高度
   
   # 可以尝试更保守的设置：
   param set FW_LND_ANG 3.0   # 减小下滑角
   param set FW_LND_AIRSPD 15.0  # 合适的着陆速度
   ```

3. **在任务中添加**：
   - 着陆前的Loiter盘旋降高（给系统稳定时间）
   - 足够长的最后进近（>500米）

## 编译和测试

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

## 测试方案

### 第1轮：测试基本飞行

**任务**：
```
起飞点 → WP1（+50米） → WP2（同高度） → WP3（同高度）
```

**观察**：
1. 是否还有触地？
2. 俯仰振荡幅度是多少？
3. 高度是否稳定？

### 第2轮：测试着陆

**任务**：
```
起飞点 → WP1（+50米） → Loiter盘旋降高 → 着陆点
```

**观察**：
1. 进近阶段是否稳定？
2. 在什么阶段失控？（进近？拉平？触地？）
3. 速度和高度变化？

## 如果问题仍然存在

### 如果俯仰仍然剧烈振荡

可能需要调整TECS的PID参数：
```bash
# 减小俯仰控制的积分增益
param set FW_T_I_GAIN_PIT 0.15  # 默认可能是0.3

# 增加时间常数，使响应更平滑
param set FW_T_TIME_CONST 8.0   # 默认可能是5.0
```

### 如果着陆仍然失控

建议：
1. 先不测试着陆，只测试航点飞行
2. 确认航点飞行稳定后，再逐步添加着陆

或者：
1. 使用NPFG模式测试（`param set FW_GUIDANCE_MODE 0`）
2. L1可能在低速着陆时不稳定

## 诊断日志

虽然移除了大部分调试输出，但系统错误仍会显示：

**关注**：
```
ERROR [fw_mode_manager] ...
WARN  [commander] Failsafe activated
WARN  [health_and_arming_checks] Attitude failure
```

**请记录**：
1. 失控发生的时刻
2. 当时的高度、速度、航向
3. 是NPFG还是L1模式
4. 失控前后的ERROR/WARN消息

## 总结

当前版本：
- ✅ 保留L1算法核心修复
- ✅ 保留简化的低高度保护（25米）
- ❌ 移除所有可能引起振荡的保护
- ❌ 移除大量调试输出

如果还有问题，请提供：
1. 俯仰振荡的具体表现（幅度、频率、阶段）
2. 着陆失控的具体时刻和现象
3. 系统日志中的ERROR/WARN信息

我们再针对性地解决！

