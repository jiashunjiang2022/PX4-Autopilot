# 🎯 用户信息带来的关键突破

## 版本
**Git**: `e0dd4471ef`

## 用户提供的关键信息

> "我在设置航点高度的时候是以ft为高度单位的，实际上的航点也就80到150ft"

## 💡 完全理解了问题

### 实际情况

```
地面海拔:     483米 AMSL
航点高度设置:  80-150 ft AGL = 24-46米 AGL
航点AMSL:     483 + 24-46 = 507-529米 AMSL ✓

飞机起飞后:    509米 AMSL
第一个航点:    ~530米 AMSL
应该:         继续爬升 21米
```

### 日志显示

```
我们发布:      alt_sp = 530.8米 AMSL ✓
飞机当前:      curr_alt = 506米 AMSL ✓
TECS内部:      alt_ref = 83-116米  ❌

问题:
TECS的altitude_reference = 83-116米
这恰好是 80-150ft 转换成米的值！
```

## 🔍 真正的Bug

**TECS被错误地用AGL值初始化，而不是AMSL值！**

### 证据链

1. **航点设置**: 80-150 ft AGL
2. **转换成米**: 24-46 m AGL
3. **TECS alt_ref**: 83-116 m（数值接近但不完全一致）
4. **应该是**: 506米 AMSL（当前高度）

### 推测

某处代码：
```cpp
// 错误的做法：
_tecs.initialize(agl_altitude, ...);  // 传了AGL！❌

// 正确的做法：
_tecs.initialize(amsl_altitude, ...); // 应该传AMSL！✓
```

## 📊 新增调试输出

现在会显示：
```
ERROR: 发布给TECS: altitude=530.8(AMSL), curr_alt=506(AMSL), home=483, AGL=23
```

这将确认：
1. 我们发布的值是正确的AMSL ✓
2. Bug一定在`fw_lateral_longitudinal_control`模块接收端 ❌

## 🎯 下一步

测试新版本，查看：
```
发布给TECS: altitude=???, curr_alt=???, home=???, AGL=???
```

如果显示：
- `home = 483m`
- `AGL = 23m`  
- `curr_alt = 506m`

那么我们发布的肯定是对的，问题100%在`fw_lateral_longitudinal_control`中TECS的初始化或更新逻辑。

## 测试

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

**请特别关注ERROR行的输出！**

