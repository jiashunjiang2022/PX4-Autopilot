# 当前版本功能说明

## 版本特点

这是一个**平衡版本**，包含必要的保护但避免过度干预。

### 核心功能

1. **L1算法修复**
   - ✅ 正确使用eta1和eta2
   - ✅ navigateWaypoint退化处理
   - ✅ navigateLine退化处理
   - ✅ 保守参数用于第一航点

2. **FOH相同高度优化**
   - ✅ 检测高度差<5米的航点
   - ✅ 相同高度时直接使用目标高度，不插值
   - ✅ 避免插值误差导致的不必要爬升/下降

3. **低高度保护**
   - ✅ AGL<50米且要求下降时，改为爬升
   - ✅ 着陆阶段自动禁用
   - ✅ 温和调整（不强制俯仰/油门）

4. **硬地线紧急保护**
   - ✅ AGL<20米时强制爬升
   - ✅ 20度俯仰 + 满油门
   - ✅ 着陆阶段自动禁用

5. **调试输出**
   - ✅ 心跳（每秒）
   - ✅ 保护触发时的WARN/ERROR

## 已解决的问题

### ✅ 问题1：触地翻转（已解决）
- **方案**：航点高度+50米 + 低高度保护
- **效果**：不再触地

### ✅ 问题2：着陆失控（应该已解决）
- **方案**：着陆阶段禁用保护
- **逻辑**：检测`next.type == SETPOINT_TYPE_LAND`

## 待解决的问题

### ❓ 问题3：俯仰角波动剧烈

**需要更多信息**：
1. 波动发生在什么高度？（>50米？20-50米？）
2. 波动幅度？（±5度？±10度？±20度？）
3. 只在L1模式还是NPFG也有？

**可能的解决方向**：
- 如果AGL>50米仍振荡 → TECS参数问题
- 如果AGL在20-50米振荡 → 保护阈值需要调整
- 如果只在L1振荡 → L1参数需要调优

## 测试建议

### 测试1：基本航点飞行（不着陆）

**任务设置**：
```
起飞点（488m）
  ↓
WP1（540m，+52米）
  ↓
WP2（540m，同高度，距离300m）
  ↓
WP3（540m，同高度，距离300m）
  ↓
返回WP1或悬停
```

**观察**：
1. 心跳输出的AGL范围
2. 保护是否触发（应该很少或不触发）
3. 俯仰角变化幅度
4. 高度是否稳定在540±2米

### 测试2：着陆测试

**任务设置**：
```
起飞点（488m）
  ↓
WP1（540m，+52米）
  ↓
Loiter盘旋降高（100m，半径50m）
  ↓
着陆点（488m，距离>500m）
```

**观察**：
1. 进近阶段是否稳定
2. 是否看到"approaching landing"的禁用信息
3. 着陆是否成功

## 关键参数检查

### 在QGC中检查

```bash
# 俯仰限制
param show FW_P_LIM_MAX  # 应该是30度
param show FW_P_LIM_MIN  # 应该是-30度

# 油门限制
param show FW_THR_MAX    # 应该是1.0
param show FW_THR_MIN    # 应该是0.0

# TECS参数
param show FW_T_SPDWEIGHT  # 速度权重，默认1.0
param show FW_T_CLMB_R_SP  # 爬升率，默认5 m/s
param show FW_T_SINK_R_SP  # 下降率，默认2 m/s

# L1参数
param show FW_L1_PERIOD    # L1周期，默认17
```

### 如果俯仰振荡严重，可以尝试

```bash
# 增大TECS时间常数，使响应更平滑
param set FW_T_TIME_CONST 8.0

# 减小俯仰积分增益
param set FW_T_I_GAIN_PIT 0.15

# 保存参数
param save
```

## 日志观察要点

### 正常飞行应该看到

```
INFO  [fw_mode_manager] === HEARTBEAT: Alt=540.0, AGL=52.0, landed=0 ===
```
→ 高度稳定，AGL>50米，无保护触发

### 如果看到保护触发

```
WARN  [fw_mode_manager] LOW ALT PROTECT: AGL=45.0, Alt SP 538.0->550.0
```
→ 说明飞行高度不够，需要提高航点高度或调整保护阈值

### 如果看到硬地线触发

```
ERROR [fw_mode_manager] !!! HARD-DECK: AGL=18.0, FORCING CLIMB !!!
```
→ 说明有严重的触地风险，需要检查任务设置

### 着陆时应该看到

```
（无保护触发日志）
```
→ 保护已在着陆阶段禁用

## 编译命令

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

## 下次反馈时请提供

1. **心跳输出显示的AGL范围** - 了解飞行高度
2. **俯仰振荡的具体表现** - 幅度、频率、发生阶段
3. **着陆失控的具体时刻** - 进近？拉平？触地？
4. **使用的模式** - NPFG还是L1
5. **保护是否触发** - 有无WARN/ERROR

这样我可以精准定位问题！

