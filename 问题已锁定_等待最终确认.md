# 问题已锁定 - 等待最终确认

## 当前状态

**Git版本**: `a63c09efb9`

## 已100%确认的事实

从你的日志：

```
行62-714: >>> PUBLISHED TO TECS: altitude=538.2, airspeed=15.0, pitch=AUTO, throttle=AUTO <<<
```

✅ **我们的代码完全正确**：
- 高度设定值：538.2米（正确）
- 空速设定值：15.0 m/s（正确）
- 控制模式：AUTO（TECS控制）

✅ **TECS收到了正确的值**

❌ **但飞机从506米持续下降到482米**

## 唯一可能的原因

### TECS被外部配置限制住了！

可能的限制：

1. **滑翔模式启用**（最可能）
   - `throttle_max = 0`  → 油门被禁用
   - `speed_weight = 2.0` → 只控制速度，完全忽略高度
   - 结果：TECS无法爬升，只能下降

2. **油门被限制**
   - `throttle_max`被设为很小的值
   - TECS无法输出足够推力

3. **TECS参数问题**
   - `FW_T_SPDWEIGHT = 2.0` → 忽略高度
   - `FW_T_CLMB_MAX`太小 → 爬升率受限
   - `FW_P_LIM_MAX`太小 → 无法抬头

## 新增调试（版本a63c09efb9）

### 1. 滑翔模式检测
```
如果启用，会看到：
ERROR [fw_mode_manager] !!! GLIDING MODE IS ENABLED IN WAYPOINT !!!
```

### 2. 油门限制
```
ERROR [fw_mode_manager] >>> THROTTLE LIMITS: min=0.00, max=0.00 <<<
或
INFO  [fw_mode_manager] >>> THROTTLE LIMITS: min=NAN, max=NAN <<<
```

## 立即测试

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean  
make px4_sitl gazebo-classic
```

## 关键观察

### 场景A：滑翔模式被启用

```
ERROR [fw_mode_manager] !!! GLIDING MODE IS ENABLED IN WAYPOINT !!!
ERROR [fw_mode_manager] >>> THROTTLE LIMITS: min=0.00, max=0.00 <<<
```

→ **这就是问题！** 在QGC中关闭航点的滑翔模式

### 场景B：油门未被限制

```
INFO  [fw_mode_manager] >>> THROTTLE LIMITS: min=NAN, max=NAN <<<
```

→ TECS有完全控制权，但仍不工作
→ 需要检查TECS参数（特别是`FW_T_SPDWEIGHT`）

## 下一步

如果看到滑翔模式警告：
1. 在QGC中编辑任务
2. 选择第一个航点
3. 取消勾选"Gliding"或"滑翔模式"
4. 重新测试

如果没有滑翔模式警告：
1. 查看`>>> THROTTLE LIMITS`显示什么
2. 在QGC Parameters中检查：
   - `FW_T_SPDWEIGHT`（应该是1.0，不是2.0）
   - `FW_T_CLMB_MAX`（应该>=3.0）
   - `FW_P_LIM_MAX`（应该>=25度）
   - `FW_THR_MAX`（应该>=0.8）

请测试并反馈看到什么调试信息！

