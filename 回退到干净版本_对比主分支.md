# 回退到干净版本 - 对比主分支

## 当前版本

**Git**: `d6dba609d3`

## 已完成的修改

按照你的要求，我对比了主分支，**移除了所有保护和调试代码**，现在代码非常接近主分支。

### 保留的修复（必要）

1. **空速NAN修复**（这是核心问题！）
   ```cpp
   // 主分支：
   const float target_airspeed = pos_sp_curr.cruising_speed > FLT_EPSILON ? pos_sp_curr.cruising_speed : NAN;
   
   // 我们的修复：
   float target_airspeed = pos_sp_curr.cruising_speed;
   if (target_airspeed < FLT_EPSILON) {
       target_airspeed = _param_fw_airspd_trim.get();  // 使用默认速度而不是NAN
   }
   ```

2. **L1算法修复**
   - `navigateL1`函数的实现
   - `navigateWaypoints`修复（使用end_waypoint避免翻转）

### 移除的所有保护

- ❌ FOH禁用逻辑
- ❌ 低高度保护
- ❌ 失速检测
- ❌ 直接俯仰/油门控制
- ❌ 所有调试输出

### 现在的代码逻辑（与主分支一致）

```cpp
// 计算FOH高度（如果有前一个航点）
position_sp_alt = FOH计算结果 或 航点高度

// 发布给TECS
longitude_sp = {
    .altitude = position_sp_alt,
    .airspeed = target_airspeed,  // 唯一区别：不会是NAN
    .pitch_direct = NAN,
    .throttle_direct = NAN
};

// 设置滑翔模式（如果启用）
if (gliding_enabled) {
    throttle_max = 0.0;
    speed_weight = 2.0;
}
```

## 测试这个干净版本

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

### 预期

1. **如果仍然下降**：
   - 说明主分支的逻辑本身有问题
   - 或者是TECS/参数问题
   - 或者是你的航点设置问题（gliding_enabled?）

2. **如果能正常飞行**：
   - 说明我们之前添加的保护干扰了TECS
   - 空速NAN修复是关键

## 下一步诊断

如果这个干净版本还下降，请检查：

### 在QGC中查看

1. **航点设置**：
   - 第一个航点是否启用了"Gliding"模式？
   - 如果启用 → 这就是问题！取消勾选

2. **Parameters**：
   ```
   FW_T_SPDWEIGHT  = ?  (应该是1.0，不是2.0)
   FW_T_CLMB_MAX   = ?  (应该>=3.0)
   FW_P_LIM_MAX    = ?  (应该>=25度)
   FW_THR_MAX      = ?  (应该>=0.8)
   ```

## Git版本对比

```bash
# 查看提交历史
git log --oneline -5

d6dba609d3 ← 当前（干净版本，接近主分支）
060f455479 ← TECS绕过方案
a63c09efb9 ← 滑翔模式调试
b037a0c04b ← 空速NAN修复（关键！）
...
```

## 核心修复

唯一保留的关键修复：**空速NAN → 使用默认速度**

这应该是让TECS能工作的最小必要修改。

请测试这个干净版本！🎯

