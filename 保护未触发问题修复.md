# 保护未触发问题 - 关键修复

## 问题发现

从日志看到：
```
AGL: 28.2 → 24.0 → 19.3 → 14.6 → 10.2 → 6.1 → 2.2 → -2.8 → -10.5
```

**飞机持续下降到负值，但保护从未触发！**

## 根因分析

### 之前的保护条件

```cpp
if (!_landed && !approaching_landing && 
    (agl < MIN_SAFE_AGL) && 
    (position_sp_alt < _current_altitude)) {  // ← 这个条件是问题！
    ...
}
```

### 问题所在

**条件4：`position_sp_alt < _current_altitude`**

这个条件要求"高度设定值低于当前高度"才触发保护。

**但实际情况可能是**：
- FOH计算出的 `position_sp_alt` 可能比 `_current_altitude` 高
- 但TECS由于其他原因（如速度控制）仍在指令下降
- 导致保护永远不触发

**举例**：
```
当前高度：490m
FOH计算：position_sp_alt = 520m (要求爬升！)
实际：飞机在下降（AGL从10m → 2m → 负值）
保护：不触发（因为520 > 490，不满足条件）
```

## 关键修复

### 修复：移除"要求下降"的判断条件

```cpp
// 之前（错误）
if (!_landed && !approaching_landing && 
    (agl < MIN_SAFE_AGL) && 
    (position_sp_alt < _current_altitude)) {  // ← 移除这个条件
    ...
}

// 现在（正确）
if (!_landed && !approaching_landing && (agl < MIN_SAFE_AGL)) {
    const float safe_altitude = _current_altitude + (MIN_SAFE_AGL - agl) + 5.0f;
    position_sp_alt = safe_altitude;  // 直接覆盖
}
```

**原理**：
- 只要AGL<30米，就强制设置安全高度
- 不管FOH计算的是什么
- 不管是爬升还是下降指令

## 添加的调试

```cpp
// 每秒或低空时输出FOH计算的结果
PX4_INFO("FOH: Alt SP=%.1f, Current=%.1f, AGL=%.1f, landing=%d",
         position_sp_alt, _current_altitude, agl, approaching_landing);
```

**目的**：
- 查看FOH实际计算的高度设定值
- 确认保护是否触发
- 诊断为什么会下降

## 预期效果

### 下次测试应该看到

**当AGL降到30米以下**：
```
INFO  [fw_mode_manager] FOH: Alt SP=520.0, Current=505.0, AGL=17.0, landing=0
WARN  [fw_mode_manager] !!! LOW ALT PROTECT: AGL=17.0 < 30.0, Alt SP 520.0 -> 548.0 !!!
INFO  [fw_mode_manager] === HEARTBEAT: Alt=507.0, AGL=19.0, landed=0 ===
INFO  [fw_mode_manager] === HEARTBEAT: Alt=512.0, AGL=24.0, landed=0 ===
INFO  [fw_mode_manager] === HEARTBEAT: Alt=520.0, AGL=32.0, landed=0 ===
```

**关键**：
- ✅ 看到保护触发的WARN
- ✅ AGL停止下降
- ✅ 开始爬升到安全高度

## 为什么之前的保护失效

可能的原因：

### 原因1：FOH计算值总是>当前高度
- FOH插值可能计算出较高的值
- 导致条件4永不满足
- 保护从不触发

### 原因2：航点高度设置导致
- 如果航点高度很高（如550m）
- 即使在下降，FOH计算值也>当前高度
- 保护失效

## 现在的逻辑

**无条件保护**：
```
AGL < 30米 → 强制设置安全高度 → TECS被迫爬升
```

**简单直接**：
- 不依赖FOH计算
- 不判断升降方向
- 只看AGL

## 编译测试

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

## 观察要点

1. **FOH调试输出**：
   - `Alt SP` 的实际数值
   - 是否比Current高或低

2. **保护触发**：
   - 当AGL<30米时，应该立即看到WARN
   - Alt SP应该跳变到一个更高的值

3. **AGL变化**：
   - 应该在触发保护后停止下降
   - 开始爬升

如果还是不触发，说明有其他问题（如_landed标志错误等）。请提供日志！


