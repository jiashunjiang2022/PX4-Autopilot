# 版本历史和回退指南

## 当前版本

**提交ID**: `09aa43acc3`
**提交信息**: Fix altitude control issues: prevent FOH from causing dangerous descent

### 这个版本的修复

1. **FOH安全限制**（关键！）
   - FOH计算的高度不会导致AGL<40米
   - 如果FOH要求下降到危险高度，强制提高

2. **移除硬地线强制爬升**
   - 避免20米临界点振荡
   - 让TECS自然控制

3. **简化低高度保护**
   - 30米阈值
   - 无条件触发（移除"要求下降"判断）
   - 温和调整

4. **着陆保护禁用**
   - 检测下一个航点是LAND类型
   - 着陆时不干预

5. **FOH相同高度检测**
   - 高度差<5米直接使用目标高度

6. **调试输出**
   - 心跳（每秒）
   - FOH计算值（每秒或低空时）

## 版本回退命令

### 回到当前版本（推荐测试版本）

```bash
cd /home/jjs/PX4/PX4-Autopilot
git reset --hard 09aa43acc3
make clean
```

### 回到上一个版本（有硬地线振荡问题）

```bash
cd /home/jjs/PX4/PX4-Autopilot
git reset --hard 9163cd1e39
make clean
```

### 回到干净的L1修复版本（会触地）

```bash
cd /home/jjs/PX4/PX4-Autopilot
git reset --hard d711cb4cd7
make clean
```

### 回到更早期的版本

```bash
# 查看所有版本
git log --oneline -20

# 回退到指定版本
git reset --hard <commit-id>
make clean
```

## 版本对比

| 版本 | 提交ID | 特点 | 问题 |
|------|--------|------|------|
| **当前** | 09aa43acc3 | FOH安全限制+温和保护 | 待测试 |
| 上一版 | 9163cd1e39 | 有硬地线强制爬升 | 20米振荡失控 |
| 干净版 | d711cb4cd7 | 只有L1算法修复 | 会触地 |

## 测试建议

### 编译

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

### 任务设置

```
起飞点：488m AMSL
WP1：550m AMSL (+62米)
WP2：550m AMSL（同高度）
WP3：550m AMSL（同高度）
着陆点：488m AMSL
```

### 观察要点

1. **心跳日志**：
   ```
   INFO  [fw_mode_manager] === HEARTBEAT: Alt=XXX, AGL=XXX, landed=0 ===
   ```
   - AGL是否稳定在>30米？

2. **FOH调试**：
   ```
   INFO  [fw_mode_manager] FOH: Alt SP=XXX, Current=XXX, AGL=XXX, landing=0
   ```
   - FOH计算的Alt SP是多少？
   - 是否会导致下降？

3. **保护触发**：
   ```
   WARN  [fw_mode_manager] !!! LOW ALT PROTECT: AGL=XXX < 30.0, Alt SP XXX -> XXX !!!
   ```
   - 低空时应该触发
   - AGL应该止跌回升

## 如果还有问题

### 问题A：仍然触地

**可能原因**：
- FOH的安全检查计算有误
- 保护阈值需要提高（30→40米）

**调试**：
- 查看FOH输出的Alt SP值
- 查看保护是否真的触发

### 问题B：俯仰振荡

**可能原因**：
- TECS参数问题
- FOH跳变过大

**解决**：
- 调整TECS参数
- 或完全禁用FOH

### 问题C：着陆失控

**可能原因**：
- 着陆检测失败（approaching_landing=false）
- 保护在着陆时仍触发

**调试**：
- 查看FOH输出的landing标志
- 确认着陆时保护是否禁用

## 快速回退

如果当前版本不行：

```bash
# 回到上一个版本
cd /home/jjs/PX4/PX4-Autopilot
git reset --hard 9163cd1e39
make clean

# 或回到干净版本
git reset --hard d711cb4cd7
make clean
```

## 查看提交历史

```bash
# 查看最近10次提交
git log --oneline -10

# 查看某次提交的详细内容
git show 09aa43acc3

# 查看某次提交修改了什么文件
git show 09aa43acc3 --stat
```

## 总结

✅ 所有修改已提交到本地git
✅ 可以随时回退到任意版本
✅ 当前版本应该解决了FOH导致下降的问题

请测试并反馈结果！


