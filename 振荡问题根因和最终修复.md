# 振荡问题根因和最终修复

## 🎯 根因确诊

从你的日志完全看清楚了问题！

### 振荡过程

```
行962: AGL=18.5m → 触发HARD-DECK(20m) → 强制20度爬升+满油门
行963: AGL=23.4m → 超过20m → 保护解除 → TECS恢复控制
行965: AGL=19.9m → 又低于20m → 再次触发HARD-DECK
行966: AGL=19.5m → 强制爬升
行967: AGL=21.4m → 保护解除
行968: AGL=20.0m → 临界点，反复触发
...
持续振荡
→ 最终姿态失败（行991）
```

### 问题核心

**硬地线阈值（20米）恰好在飞行高度范围内！**

- 飞机自然飞行高度在18-23米
- 20米阈值在这个范围的中间
- 导致频繁触发和解除
- 强制20度俯仰 ↔ TECS控制 来回切换
- **系统无法稳定**

## 最终修复

### 修复1：移除硬地线强制爬升

**原因**：
- 硬地线的"强制俯仰+油门"太激进
- 容易造成临界高度振荡
- TECS的温和控制已经足够

**方案**：
```cpp
// 只保留低高度保护（温和调整高度设定值）
// 移除硬地线强制爬升
const float MIN_SAFE_AGL = 30.0f;  // 统一阈值

if (!_landed && !approaching_landing && (agl < 30.0f) && (position_sp_alt < _current_altitude)) {
    position_sp_alt = _current_altitude + (30.0f - agl) + 5.0f;
}
```

### 修复2：提高航点高度到足够高

**问题**：
- 你的航点高度可能只比起飞点高20-30米
- FOH插值导致中途高度更低
- 触发保护

**解决**：
- 第一个航点：起飞点 + **60米以上**
- 其他航点：保持相同高度或更高

## 为什么这次修复应该有效

### 去掉硬地线的好处

1. **无强制俯仰切换**
   - TECS始终控制俯仰
   - 响应平滑连续

2. **无临界振荡**
   - 不存在"触发/解除"的临界点
   - 低高度保护只改高度设定值，让TECS自然响应

3. **系统更简单**
   - 只有一个保护机制
   - 逻辑清晰

### 保留低高度保护的作用

- 30米阈值合理（不会太高）
- 温和调整（不强制）
- 着陆时禁用（不干扰）

## 编译测试

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

## 预期效果

### 正常飞行（AGL>30米）

```
INFO  [fw_mode_manager] === HEARTBEAT: Alt=550.0, AGL=62.0, landed=0 ===
```
→ 高度稳定，无保护触发，俯仰平滑

### 略低高度（AGL在30米附近）

```
INFO  [fw_mode_manager] === HEARTBEAT: Alt=520.0, AGL=32.0, landed=0 ===
INFO  [fw_mode_manager] === HEARTBEAT: Alt=521.0, AGL=33.0, landed=0 ===
```
→ 保护可能偶尔触发，但只改高度设定值，TECS平滑响应

### 着陆阶段

```
INFO  [fw_mode_manager] === HEARTBEAT: Alt=495.0, AGL=7.0, landed=0 ===
```
→ 保护已禁用，正常着陆流程

### 不应该再看到

```
❌ ERROR [fw_mode_manager] !!! HARD-DECK: ...
❌ AGL在19-21米反复振荡
❌ Attitude failure
```

## 航点设置建议

```
起飞点：488m AMSL
  ↓
WP1：550m AMSL（+62米）← 重要！要足够高
  ↓
WP2：550m AMSL（同高度）
  ↓
WP3：550m AMSL（同高度）
  ↓
着陆点：488m AMSL
```

## 总结

**核心修复**：
- ✅ 移除硬地线强制爬升（避免临界振荡）
- ✅ 保留温和的低高度保护（30米）
- ✅ 着陆阶段禁用保护
- ✅ FOH相同高度检测
- ✅ 心跳调试

**关键**：
- 🔴 第一个航点高度要足够高（+60米以上）
- 🔴 不要让FOH计算的中途高度低于30米

请重新测试！应该彻底解决振荡问题了。

