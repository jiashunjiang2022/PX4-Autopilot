# 当前版本的两个问题

## 当前版本特点

包含：
- ✅ L1算法核心修复
- ✅ FOH相同高度检测（<5米不插值）
- ✅ 低高度保护（50米）
- ✅ 硬地线保护（20米）
- ✅ 心跳调试输出

## 问题1：飞行过程中俯仰角波动剧烈

### 现象
在飞往航点的过程中，俯仰角变化很大

### 可能原因

#### 原因A：保护触发导致的振荡
从之前的日志看到：
```
AGL在29-31米之间振荡
持续触发LOW ALT PROTECT
持续触发HARD-DECK
```

这说明：
- 飞行高度在20-50米之间
- 保护频繁介入
- 造成俯仰振荡

#### 原因B：TECS参数不合适
- 俯仰PID增益过大
- 高度和速度权重不平衡
- 响应过快

### 解决方向

**方向1：如果飞行高度>50米仍振荡**
→ TECS参数问题，需要调参

**方向2：如果飞行高度在20-50米才振荡**
→ 保护触发导致，需要调整保护逻辑

**方向3：如果只在L1模式振荡，NPFG正常**
→ L1算法参数问题

## 问题2：飞往着陆点时失控

### 现象
在着陆阶段发生失控

### 可能原因

#### 原因A：低高度保护在着陆时错误触发
```cpp
if (!_landed && (agl < MIN_SAFE_AGL) && (position_sp_alt < _current_altitude))
```

**问题**：
- 着陆时高度自然下降
- `position_sp_alt < _current_altitude` 总是成立
- 保护会强制改为爬升
- **与着陆指令冲突！**

#### 原因B：硬地线在着陆拉平时触发
- 着陆拉平高度可能<20米
- 触发硬地线强制爬升
- 破坏着陆流程

### 解决方向

**必须**：在着陆模式下禁用保护！

## 立即修复：禁用着陆时的保护

需要检测当前是否在着陆模式：
```cpp
// 检查是否在着陆模式
const bool is_landing = (_control_mode_current == FW_POSCTRL_MODE_AUTO_LANDING_STRAIGHT) ||
                        (_control_mode_current == FW_POSCTRL_MODE_AUTO_LANDING_CIRCULAR);

// 只在非着陆模式时启用保护
if (!_landed && !is_landing && (agl < MIN_SAFE_AGL) && ...) {
    ...
}
```

但是`control_auto_position`函数中可能无法访问`_control_mode_current`。

**替代方案**：检查航点类型
```cpp
// 检查下一个航点是否是着陆点
const bool approaching_landing = (_position_setpoint_next_valid && 
                                  pos_sp_next.type == position_setpoint_s::SETPOINT_TYPE_LAND);

// 非着陆阶段才启用保护
if (!_landed && !approaching_landing && (agl < MIN_SAFE_AGL) && ...) {
    ...
}
```

## 需要的调试信息

为了更好地诊断，需要你提供：

### 对于问题1（俯仰振荡）

1. **振荡发生的高度范围**？
   - 如果AGL>50米仍振荡 → TECS问题
   - 如果AGL在20-50米振荡 → 保护问题

2. **振荡幅度**？
   - ±5度以内：可接受
   - ±10度：需要调参
   - ±20度以上：严重问题

3. **只在L1模式还是NPFG也有**？

### 对于问题2（着陆失控）

1. **失控发生在什么阶段**？
   - 进近下滑（高度>50米）
   - 接近地面（高度20-50米）
   - 拉平阶段（高度<20米）

2. **失控的表现**？
   - 突然爬升（保护触发）
   - 横滚失控
   - 俯仰失控

3. **着陆航点设置**？
   - 前一个航点到着陆点的距离
   - 着陆点高度设置
   - 是否有Loiter降高

## 快速测试建议

### 测试1：不测试着陆，只测试航点飞行

任务：
```
起飞 → WP1（+60米）→ WP2（同高度）→ WP3（同高度）→ 返回WP1（悬停）
```

**目的**：
- 隔离问题
- 只测试俯仰振荡
- 不涉及着陆

### 测试2：单独测试着陆

任务：
```
起飞 → WP1（+60米）→ Loiter盘旋 → 着陆点
```

**目的**：
- 单独诊断着陆问题
- 观察保护是否在着陆时触发

## 下次回复时请提供

1. 从心跳日志看到的AGL范围
2. 俯仰振荡发生的具体阶段
3. 着陆失控的具体现象
4. 完整的终端日志（如果可能）

这样我可以更准确地定位和解决问题！

