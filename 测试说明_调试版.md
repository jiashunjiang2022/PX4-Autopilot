# 调试版本测试说明

## 当前版本

**提交ID**: `bf8b09b25f`

## 这个版本的作用

专门诊断**为什么TECS不响应保护修改的高度设定值**。

## 新增的调试输出

### 1. PUBLISHED调试
```
INFO  [fw_mode_manager] PUBLISHED to TECS: Alt SP=XXX.X, pitch=TECS, throttle=TECS
```
显示：
- 实际发布给TECS的高度设定值
- 俯仰/油门是直接控制还是TECS控制

### 2. 滑翔模式检测
```
ERROR [fw_mode_manager] !!! GLIDING MODE DETECTED - FORCING DISABLE FOR SAFETY !!!
```
显示：
- 航点是否启用了滑翔模式
- 滑翔模式会禁用油门（导致下滑）

### 3. 油门限制调试
```
INFO  [fw_mode_manager] Throttle limits: min=NAN, max=NAN
```
显示：
- TECS的油门范围限制
- 如果max=0，则无法爬升

## 测试步骤

```bash
cd /home/jjs/PX4/PX4-Autopilot
make clean
make px4_sitl gazebo-classic
```

## 重点观察

### 关键1：PUBLISHED输出

**如果看到**：
```
PUBLISHED to TECS: Alt SP=523.2, pitch=TECS, throttle=TECS
```
→ 说明保护成功修改并发布了523.2米

**然后观察**：
- Current是否开始爬升？
- 如果不爬升 → TECS接收问题或参数问题

### 关键2：滑翔模式

**如果看到**：
```
ERROR [fw_mode_manager] !!! GLIDING MODE DETECTED - FORCING DISABLE FOR SAFETY !!!
```
→ **这就是问题所在！** 滑翔模式禁用了油门，飞机只能下滑

**解决**：
- 在QGC中检查航点设置
- 确保所有航点的"滑翔模式"未勾选

### 关键3：油门限制

**如果看到**：
```
Throttle limits: min=SET, max=SET
或
Throttle limits: min=0.0, max=0.0
```
→ 油门被限制了，无法爬升

## 预期的正常输出

```
INFO  [fw_mode_manager] === HEARTBEAT: Alt=496.0, AGL=8.0, landed=0 ===
INFO  [fw_mode_manager] FOH: Alt SP=549.1, Current=496.0, AGL=8.0, landing=0
WARN  [fw_mode_manager] !!! LOW ALT PROTECT: AGL=8.0 < 30.0, Alt SP 549.1 -> 531.0 !!!
INFO  [fw_mode_manager] PUBLISHED to TECS: Alt SP=531.0, pitch=TECS, throttle=TECS
INFO  [fw_mode_manager] Throttle limits: min=NAN, max=NAN
INFO  [fw_mode_manager] === HEARTBEAT: Alt=498.0, AGL=10.0, landed=0 ===  ← 开始爬升！
INFO  [fw_mode_manager] === HEARTBEAT: Alt=505.0, AGL=17.0, landed=0 ===
INFO  [fw_mode_manager] === HEARTBEAT: Alt=520.0, AGL=32.0, landed=0 ===  ← 爬到安全高度
```

## 如果问题仍然存在

### 情况A：看到滑翔模式警告

→ 问题确诊！在QGC中禁用滑翔模式

### 情况B：PUBLISHED显示正确值，但仍下降

→ TECS控制器本身的问题，需要检查：
- TECS参数（爬升率、油门限制等）
- TECS订阅是否正常
- 是否有其他模块覆盖了TECS

### 情况C：油门被限制（max=0或很小）

→ 找出是哪里设置了油门限制

## Git版本管理

```bash
# 当前版本（调试版）
git log -1  # 应该显示 bf8b09b25f

# 如果需要回退
git reset --hard 09aa43acc3  # 上一版本（无额外调试）
git reset --hard 9163cd1e39  # 更早版本
```

## 下次反馈请提供

1. 是否看到"GLIDING MODE DETECTED"？
2. PUBLISHED显示的Alt SP是多少？
3. Throttle limits显示的是什么？
4. Current是否有任何爬升趋势？

这样我可以精确定位问题！


