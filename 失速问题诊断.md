# 失速问题 - 真正的根因！

## 🔴 症状分析

从你提供的日志：
```
HEARTBEAT: Alt=506.8, AGL=18.6  ← 起飞后
HEARTBEAT: Alt=510.8, AGL=22.7  ← 爬到最高点
HEARTBEAT: Alt=510.0, AGL=21.9  ← 开始下降
HEARTBEAT: Alt=506.3, AGL=18.1  ← 持续下降
HEARTBEAT: Alt=501.4, AGL=13.2  ← 加速下降
HEARTBEAT: Alt=496.8, AGL=8.7   ← 危险！
```

**关键观察**：
1. 保护一直在触发：`Alt SP 530.8->538.2`（要求爬到538米）
2. 但飞机从510米降到496米
3. **你说机翼有向上的力，但飞机还在下降**

## ✅ 诊断：这是失速！

### 失速的典型特征

1. **升力存在但不足**：
   - ✅ 机翼有向上的力（你在Gazebo看到的）
   - ✅ 但升力 < 重力
   - ✅ 所以飞机下降

2. **TECS指令爬升但无效**：
   - ✅ 保护要求爬到538米
   - ✅ TECS拉高机头
   - ✅ 但拉高 → 速度更慢 → 升力更小 → 下降更快

3. **从最高点快速下降**：
   - ✅ 起飞后短暂爬升到22.7米
   - ✅ 然后快速下降（失速下沉率：~1-2 m/s）

### 失速的根本原因

**升力公式**：`Lift = 0.5 * ρ * V² * S * CL`

关键：**升力与速度的平方成正比**

如果速度太慢：
- V=10 m/s → V² = 100
- V=8 m/s → V² = 64 （升力下降36%！）
- V=6 m/s → V² = 36 （升力下降64%！）

**恶性循环**：
```
速度慢 → 升力不足 → 开始下降
↓
TECS拉高机头试图爬升
↓
机头抬高 → 阻力增加 → 速度更慢
↓
升力更小 → 下降更快
↓
进入失速螺旋
```

## 🔧 已实施的修复

### 1. 失速检测
```cpp
const float MIN_SAFE_AIRSPEED = FW_AIRSPD_MIN * 1.2;
if (airspeed < MIN_SAFE_AIRSPEED && descending && AGL < 50m) {
    PX4_ERR("STALL DETECTED!");
}
```

### 2. 低空最小速度保护
```cpp
if (target_airspeed < FW_AIRSPD_MIN * 1.15 && AGL < 100m) {
    target_airspeed = FW_AIRSPD_MIN * 1.15;  // 强制提高速度设定值
}
```

### 3. 增强调试
```cpp
HEARTBEAT: Alt=X, AGL=X, Airspeed=X, Vz=X
```

现在会显示：
- **Airspeed**：空速（关键指标！）
- **Vz**：垂直速度（正=下降，负=上升）

## 🧪 测试并验证

```bash
cd /home/jjs/PX4/PX4-Autopilot
make px4_sitl gazebo-classic
```

### 观察重点

1. **空速值**（最关键！）
   ```
   正常：Airspeed=15-20 m/s
   危险：Airspeed<12 m/s （可能失速）
   致命：Airspeed<10 m/s （肯定失速）
   ```

2. **垂直速度Vz**
   ```
   爬升：Vz=-1.0 （负值）
   平飞：Vz=0.0
   下降：Vz=+1.0 （正值）
   失速下降：Vz=+2.0以上
   ```

3. **失速警告**
   ```
   如果看到：
   ERROR [fw_mode_manager] !!! STALL DETECTED: Airspeed=8.5 < 12.0, Descending 1.5 m/s
   → 确诊失速！
   ```

4. **空速保护触发**
   ```
   如果看到：
   WARN [fw_mode_manager] Low altitude: increasing airspeed SP 10.0 -> 11.5
   → 保护正在工作
   ```

## 📊 预期效果

### 如果确实是失速

**之前**（失速）：
```
Takeoff detected
Alt=506.8, AGL=18.6, Airspeed=9.5 ← 速度太慢！
Alt=510.8, AGL=22.7, Airspeed=8.2 ← 更慢了，爬到最高点
Alt=510.0, AGL=21.9, Airspeed=7.8 ← 失速开始
STALL DETECTED: Airspeed=7.8 < 12.0
Alt=501.4, AGL=13.2, Airspeed=8.5 ← 下降中速度恢复
Alt=496.8, AGL=8.7, Airspeed=9.2
```

**现在**（有保护）：
```
Takeoff detected
Alt=506.8, AGL=18.6, Airspeed=9.5
Low altitude: increasing airspeed SP 10.0 -> 11.5 ← 保护触发
Alt=510.8, AGL=22.7, Airspeed=11.8 ← 保持安全速度
Alt=515.0, AGL=27.0, Airspeed=12.2 ← 继续爬升
Alt=525.0, AGL=37.0, Airspeed=13.5 ← 平稳爬升
```

## 💡 如果不是失速

如果日志显示：
- Airspeed=15-20 m/s（正常）
- 但仍然下降

那么可能是：
1. **TECS参数问题**（俯仰/油门增益）
2. **推力不足**（油门限制、电机问题）
3. **气动问题**（飞机太重、翼载荷太大）

## 🎯 立即测试

请运行测试并**特别关注Airspeed值**！

如果Airspeed<12 m/s，那就确认是失速。
如果Airspeed>15 m/s但还下降，那是其他问题。

请反馈测试结果，特别是**起飞后的Airspeed值**！

